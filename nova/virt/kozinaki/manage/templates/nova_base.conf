{{ provider_config }}


[DEFAULT]
host={{ hostname }}
state_path=/var/lib/{{ hostname }}
compute_driver={{ compute_driver }}
log_file={{ hostname }}.log


[oslo_concurrency]
lock_path=/var/lib/{{ hostname }}/tmp